{
  "name": "medcare-backend",
  "version": "1.0.0",
  "description": "MedCare REST API — SQL Server backend",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node scripts/free-port.js && node server.js",
    "dev": "node scripts/free-port.js && node --watch server.js",
    "db:init": "node scripts/init-db.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mssql": "^11.0.1",
    "uuid": "^11.1.0"
  }
}
